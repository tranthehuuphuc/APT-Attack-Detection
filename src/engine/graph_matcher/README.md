## Graph Matching Engine

This project expects an engine implementation to be placed at:

`src/engine/graph_matcher/engine_repo/`

The engine is invoked via `src/engine/megr_adapter.py` using a subprocess call.
